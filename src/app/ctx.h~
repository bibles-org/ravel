#pragma once

#include <core/target.h>
#include <core/analysis/strings.h>
#include <memory>

namespace app {
    extern std::unique_ptr<core::target> active_target;
    extern std::unique_ptr<core::analysis::strings_analyzer> strings;
};
